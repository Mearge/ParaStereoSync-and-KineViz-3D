# Screenshots and Figures

Place screenshots and figures for the documentation in this folder as image files (PNG, JPG). Name files clearly, e.g., `calibration_gui.png`, `tracking_overlay.png`, `triangulation_result.png`.

When ready, update the corresponding Markdown docs to reference these images, for example:

```markdown
![Calibration GUI](images/calibration_gui.png)
```
